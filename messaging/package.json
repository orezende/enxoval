{
  "name": "@enxoval/messaging",
  "version": "1.0.5",
  "description": "KafkaJS wrapper exposing publish, consume, ensureTopics and the enxoval CLI",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "enxoval": "dist/cli/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "create-topic": "node dist/cli/index.js create-topic"
  },
  "keywords": [
    "kafka",
    "messaging",
    "typescript"
  ],
  "license": "MIT",
  "dependencies": {
    "kafkajs": "^2.2.4",
    "@enxoval/observability": "^1.0.0",
    "@enxoval/types": "^1.0.25"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
