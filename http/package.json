{
  "name": "@enxoval/http",
  "version": "1.0.26",
  "description": "Fastify wrapper exposing get, post, html, listen and inject helpers",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "kanly": "dist/cli/kanly.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "fastify",
    "http",
    "typescript"
  ],
  "license": "MIT",
  "dependencies": {
    "@enxoval/observability": "^1.0.0",
    "@enxoval/types": "^1.0.6",
    "@fastify/cors": "^11.2.0",
    "fastify": "^5.8.5"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.2"
  }
}
